<%= form_for @estoque do |f| %>

    <% if @estoque.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@estoque.errors.count, "error") %> prohibited
            this article from being saved:
          </h2>
          <ul>
            <% @estoque.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= f.label :nome %><br>
      <%= f.text_field :nome %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>